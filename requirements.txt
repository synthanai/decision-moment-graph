# DMG Open Standard - Requirements

# Core (minimal install)
numpy<2
agentic-kit>=0.1.0

# Optional: Semantic memory with embeddings
# pip install dmg-open-standard[embeddings]
sentence-transformers>=2.0.0

# Optional: HTTP dispatcher
# pip install dmg-open-standard[http]
requests>=2.28.0

# Development
pytest>=7.0.0
pytest-cov>=4.0.0
mcp>=1.0.0
